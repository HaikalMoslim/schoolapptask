<h1 class="mb-5"><%= @student.name %></h1>

<%= link_to 'Edit', edit_student_path(@student) %> |
<%= link_to 'Back', students_path %> |
<%= link_to 'Delete', student_path(@student), data:{
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
} %>
